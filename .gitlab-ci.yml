include:
  - https://gitlab.com/ubports/porting/community-ports/halium-generic-adaptation-build-tools/-/raw/main/gsi-port-ci.yml

build:
  # Post-build steps to duplicate the built recovery into rootfs as well. This
  # will be used by `device-hacks` script to update recovery post-boot.
  # TODO: remove once 24.04-1.x goes EOL.

  # XXX: I can't extend `scripts` section, but can define `after_script`.
  # `after_script` failure does not fail the job. I'm not sure if it's worth it
  # To dig further into this...
  after_script:
    - ./include-recovery-in-rootfs.sh
